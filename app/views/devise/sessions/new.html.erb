<style type="text/css">
  html, body {
    height: 100%;
  }
</style>

<div id="wrapper" class="background-image">

<div class="twelve columns ">
  <div class="logo-caption">
    <a href="/"><div class="logo" ></div></a>
  </div>
  <h4 style="color:white; text-align:center; margin: 50px 0 0 0;">SIGN IN</h4>

<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <span class="input input--nao">
    <%= f.text_field :login, :class => 'input__field input__field--nao', :id => 'input-1' %>
    <label class="input__label input__label--nao" for="input-1">
      <span class="input__label-content input__label-content--nao">Login ID:</span>
    </label>
    <svg class="graphic graphic--nao" width="300%" height="100%" viewBox="0 0 1200 60" preserveAspectRatio="none">
      <path d="M0,56.5c0,0,298.666,0,399.333,0C448.336,56.5,513.994,46,597,46c77.327,0,135,10.5,200.999,10.5c95.996,0,402.001,0,402.001,0"/>
    </svg>
  </span> 

  <span class="input input--nao">
    <%= f.password_field :password, autocomplete: "off", :class => 'input__field input__field--nao', :id => 'input-1' %>
    <label class="input__label input__label--nao" for="input-1">
      <span class="input__label-content input__label-content--nao">Password:</span>
    </label>
    <svg class="graphic graphic--nao" width="300%" height="100%" viewBox="0 0 1200 60" preserveAspectRatio="none">
      <path d="M0,56.5c0,0,298.666,0,399.333,0C448.336,56.5,513.994,46,597,46c77.327,0,135,10.5,200.999,10.5c95.996,0,402.001,0,402.001,0"/>
    </svg>
  </span> 

  <% if devise_mapping.rememberable? -%>
    <div class="checkbox-field">
      <%= f.check_box :remember_me, :class => "float-left check" %>
      <%= f.label :remember_me, :class => "float-left check-label" %>
    </div>
  <% end -%>

  <%= f.submit "Log in", :class => 'button blue-button reg-button-margin' %>
<% end %>

<script>
  (function() {
    // trim polyfill : https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/String/Trim
    if (!String.prototype.trim) {
      (function() {
        // Make sure we trim BOM and NBSP
        var rtrim = /^[\s\uFEFF\xA0]+|[\s\uFEFF\xA0]+$/g;
        String.prototype.trim = function() {
          return this.replace(rtrim, '');
        };
      })();
    }

    [].slice.call( document.querySelectorAll( 'input.input__field' ) ).forEach( function( inputEl ) {
      // in case the input is already filled..
      if( inputEl.value.trim() !== '' ) {
        classie.add( inputEl.parentNode, 'input--filled' );
      }

      // events:
      inputEl.addEventListener( 'focus', onInputFocus );
      inputEl.addEventListener( 'blur', onInputBlur );
    } );

    function onInputFocus( ev ) {
      classie.add( ev.target.parentNode, 'input--filled' );
    }

    function onInputBlur( ev ) {
      if( ev.target.value.trim() === '' ) {
        classie.remove( ev.target.parentNode, 'input--filled' );
      }
    }
  })();
</script> 
<div class="shared-links">
<%= render "devise/shared/links" %>
</div>
</div>
